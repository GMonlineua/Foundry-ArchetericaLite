<form class="{{cssClass}} {{actor.type}} flexcol" autocomplete="off">

  {{!-- Sheet Header --}}
  <header class="sheet-header">
    <div class="img-block">
      <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}" height="100" width="100"/>
      <div class=" hp-bar resource">
          <progress id="system.hp.value" value="{{system.hp.value}}" max="{{system.hp.max}}" title="{{localize 'ARCHETERICALITE.Hp'}}"></progress>
          <div class="hp-input">
            <input class="clear-input" type="number" name="system.hp.value" value="{{system.hp.value}}" data-dtype="Number"/>
            <span> / </span>
            <input class="clear-input" type="number" name="system.hp.max" value="{{system.hp.max}}" data-dtype="Number"/>
          </div>
        </div>
    </div>
    <div class="header-fields">
      <h1 class="charname"><input name="name" type="text" value="{{actor.name}}" placeholder="Name"/></h1>

      <h2 class="faction">
        <label for="system.faction" class="resource-label">{{localize 'ARCHETERICALITE.Faction'}}: </label>
        <input class="clear-input" type="text" name="system.faction" value="{{system.faction}}" data-dtype="String"/>
      </h2>

      <div class="resources flexrow">
        <div class="resource">
          <label for="system.session" class="resource-label">{{localize 'ARCHETERICALITE.Session'}}: </label>
          <input type="number" name="system.session" value="{{system.session}}" data-dtype="Number"/>
        </div>

        <div class="resource">
          <label for="system.ups" class="resource-label">{{localize 'ARCHETERICALITE.FreeUp'}}: </label>
          <input type="number" name="system.ups" value="{{system.ups}}" data-dtype="Number"/>
        </div>

        <div class="resource">
          <label for="system.infirmity" class="resource-label">{{localize 'ARCHETERICALITE.Infirmity'}}: </label>
          <input type="number" name="system.infirmity" value="{{system.infirmity}}" data-dtype="Number"/>
        </div>
      </div>

      <div class="resources flexrow">
        <div class="resource">
          <label for="system.stress" class="resource-label">{{localize 'ARCHETERICALITE.Stress'}}: </label>
          <input type="number" name="system.stress" value="{{system.stress}}" data-dtype="Number"/>
        </div>

        <div class="resource">
          <label for="system.deepstress" class="resource-label">{{localize 'ARCHETERICALITE.DeepStress'}}: </label>
          <input type="number" name="system.deepstress" value="{{system.deepstress}}" data-dtype="Number"/>
        </div>

        <div class="resource flexrow">
          <label for="system.health.value" class="resource-label">{{localize 'ARCHETERICALITE.Health'}}: </label>
          <select name="system.health" data-type="String">
            {{#select system.health}}
            <option value="healthy">{{localize 'ARCHETERICALITE.Healthy'}}</option>
            <option value="wounded">{{localize 'ARCHETERICALITE.Wounded'}}</option>
            <option value="severely-wounded">{{localize 'ARCHETERICALITE.SeverelyWounded'}}</option>
            <option value="critical-condition">{{localize 'ARCHETERICALITE.CriticalCondition'}}</option>
            <option value="at-death">{{localize 'ARCHETERICALITE.AtDeath'}}</option>
            {{/select}}
          </select>
        </div>
      </div>
    </div>
  </header>

  {{!-- Sheet Tab Navigation --}}
  <nav class="sheet-tabs tabs flexrow" data-group="primary">
    {{!-- Default tab is specified in actor-sheet.mjs --}}
    <a class="item" data-tab="narrative">{{localize 'ARCHETERICALITE.NavNarrative'}}</a>
    <a class="item" data-tab="combat">{{localize 'ARCHETERICALITE.NavCombat'}}</a>
    <a class="item" data-tab="mystical">{{localize 'ARCHETERICALITE.NavMystical'}}</a>
    <a class="item" data-tab="riches">{{localize 'ARCHETERICALITE.NavRiches'}}</a>
    <a class="item" data-tab="description">{{localize 'ARCHETERICALITE.NavDescription'}}</a>
  </nav>

  {{!-- Sheet Body --}}
  <section class="sheet-body">

    {{!-- Owned Narrative Tab --}}
    <div class="tab narrative" data-group="primary" data-tab="narrative">
      {{> "systems/archetericalite/templates/actor/tabs/actor-narrative.hbs"}}
    </div>

    {{!-- Owned Combat Tab --}}
    <div class="tab combat" data-group="primary" data-tab="combat">
       {{> "systems/archetericalite/templates/actor/tabs/actor-combat.hbs"}}
    </div>

    {{!-- Owned Mystical Tab --}}
    <div class="tab mystical" data-group="primary" data-tab="mystical">
      {{> "systems/archetericalite/templates/actor/tabs/actor-mystical.hbs"}}
    </div>

    {{!-- Owned Riches Tab --}}
    <div class="tab riches" data-group="primary" data-tab="riches">
      {{> "systems/archetericalite/templates/actor/tabs/actor-riches.hbs"}}
    </div>

    {{!-- Owned Description Tab --}}
    <div class="tab description" data-group="primary" data-tab="description">
      {{> "systems/archetericalite/templates/actor/tabs/actor-description.hbs"}}
    </div>

  </section>
</form>

